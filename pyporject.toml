[project]
name = "nautobot-app-cli"
version = "0.1.0"
description = "Simple Nautobot App Manager"
requires-python = ">=3.10"

dependencies = [
    "click"
]

[project.scripts]
nautobot-app = "nautobot_app.cli:cli"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"